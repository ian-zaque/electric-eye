const sensor = {
    type_sensor: '',
    model: '',
    description: '',
}

const sensorsList = []

const isLoading = false

const errorsSensors = {}

export default {
    sensor,
    sensorsList,
    isLoading,
    errorsSensors,
}