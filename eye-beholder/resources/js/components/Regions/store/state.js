const region = {
    name: "",
    description: "",
}
const regionsList = []
const isLoading = false
const errorsRegions = {}

export default {
    region,
    regionsList,
    isLoading,
    errorsRegions,
}